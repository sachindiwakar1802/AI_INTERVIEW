{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd FRONTEND/frontend && npm install && npm run build && npx serve -s dist"
  }
}