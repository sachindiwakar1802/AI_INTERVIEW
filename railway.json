{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd FRONTEND/frontend && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd FRONTEND/frontend && npx serve -s dist"
  }
}